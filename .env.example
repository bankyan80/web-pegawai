# Salin file ini menjadi ".env" lalu sesuaikan nilainya.
# Jika ".env" tidak ada, aplikasi memakai nilai default (root / tanpa password).

DB_NAME=dbtechmuda42
DB_HOST=localhost
DB_USER=root
DB_PASS=

# set ke 1 hanya untuk development (menampilkan pesan error di browser)
APP_DEBUG=0
